{
  "name": "@ai4s/claude-bridge",
  "version": "0.1.9",
  "private": true,
  "type": "module",
  "description": "OpenCode-compatible HTTP+SSE bridge that runs the agent on the Claude Agent SDK. Accepts the same CLI/env contract as `opencode serve`, so the desktop shell and the web server can spawn it as a drop-in sidecar.",
  "bin": {
    "claude-bridge": "./src/server.mjs"
  },
  "scripts": {
    "start": "node src/server.mjs serve --hostname 127.0.0.1 --port 4096"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.206"
  }
}
