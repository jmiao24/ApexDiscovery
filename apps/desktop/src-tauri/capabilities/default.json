{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window baseline. start-dragging is NOT part of core:default — without it every data-tauri-drag-region (overlay titlebar strips) is silently denied and the window cannot be moved.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
